stages:
  - test
  - build
  - deploy

default:
  image: node:18

cache:
  paths:
    - node_modules/

test:
  stage: test
  script:
    - echo "No tests yet. Placeholder job."

build:
  stage: build
  script:
    - echo "Build placeholder."

deploy:
  stage: deploy
  script:
    - echo "Deployment will be configured later."
  when: manual
